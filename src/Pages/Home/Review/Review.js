import React from 'react';

const Review = ({review}) => {
    const {avatar, name, location, comment} = review;
    return (
        <div>
            
        </div>
    );
};

export default Review;